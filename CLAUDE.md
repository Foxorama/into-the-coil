# Into the Coil — working notes for Claude

**There is no constitution yet, deliberately.** It is waiting on a design decision, and
writing conventions before that lands means writing them twice.

**Read `docs/scaffold-plan.md` before proposing anything** — the commit sequence, the
decisions already taken, and the architectural commitments that constrain them. It is
gitignored working material: it exists on this machine, not in a fresh clone.

`C:\Golf-Stars` (The Far Carry) is the predecessor project. **Its patterns transfer; its
content does not** — it is a golf game, and its simulation layer is fused to that domain at
the type level. Read named files for a named reason. Never browse it for inspiration.
